---
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/header.astro';
import Footer from '@components/footer.astro';
import TechnologyCard from '@components/technology-card.astro';
import { getCollection } from 'astro:content';

const allTechnologies = await getCollection('technologies').then((technologies) =>
  [...technologies].sort(
    (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
  )
);
---

<BaseLayout>
  <Header/>
  
  <!-- Breadcrumbs -->
  <nav class="bg-white border-b border-stone-200 py-3">
    <div class="container mx-auto px-4">
      <ol class="flex text-stone-600 text-sm">
        <li><a href="/" class="hover:text-orange-600">Главная</a></li>
        <li class="mx-2">/</li>
        <li class="text-stone-900">Технологии</li>
      </ol>
    </div>
  </nav>

  <main class="bg-stone-50 py-12">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-stone-800 mb-4">Технологии стабилизации грунтов</h1>
        <p class="text-stone-600 max-w-3xl mx-auto text-lg">
          Инновационные методы укрепления грунтов для различных сфер применения. 
          Мы используем современные технологии и проверенные решения для достижения максимального результата.
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {allTechnologies.map((technology) => (
          <TechnologyCard technology={technology} />
        ))}
      </div>
      
      {allTechnologies.length === 0 && (
        <div class="text-center py-12">
          <p class="text-stone-600 text-lg">Информация о технологиях скоро появится</p>
        </div>
      )}

      <!-- Преимущества наших технологий -->
      <div class="bg-white rounded-xl p-8 mt-16">
        <h2 class="text-2xl font-bold text-stone-800 mb-6 text-center">Преимущества наших технологий</h2>
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="text-center">
            <div class="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-award text-orange-500 text-2xl"></i>
            </div>
            <h3 class="font-semibold mb-2">Высокое качество</h3>
            <p class="text-stone-600 text-sm">Используем только проверенные материалы и технологии</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-leaf text-orange-500 text-2xl"></i>
            </div>
            <h3 class="font-semibold mb-2">Экологичность</h3>
            <p class="text-stone-600 text-sm">Безопасные для окружающей среды решения</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-clock text-orange-500 text-2xl"></i>
            </div>
            <h3 class="font-semibold mb-2">Быстрота выполнения</h3>
            <p class="text-stone-600 text-sm">Оптимизированные процессы и современное оборудование</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-chart-line text-orange-500 text-2xl"></i>
            </div>
            <h3 class="font-semibold mb-2">Долговечность</h3>
            <p class="text-stone-600 text-sm">Результат сохраняется на долгие годы</p>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <Footer/>
</BaseLayout>